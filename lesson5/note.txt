Потоки

Глобал интерлок

args=(2, ) - сюда передаем параметры функции

kwargs={} - именованый
dict()

join - заставляет ждать программу

демон - не ждет выполнения функция. Игнорируется при завершении основного потока.

Потоки не киляются.

Context manager class == enter exit

дает возможность использовать with as