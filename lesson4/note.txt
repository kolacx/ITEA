property - get set

classmethod - для работы с атребутами не трогает сам обьект

static - вспомогательная функция. Функция не влияет на поведение обькта по логике вписывается в сам класс.

Вызывать можем не создавая обьектов


метод __call__

zip - склеивает [][]